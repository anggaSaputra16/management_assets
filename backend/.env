# Database
# Default: connect to the Dockerized Postgres service named `postgres` when
# running via docker-compose. If you run the backend directly on your host and
# want to connect to a local Postgres instance, change this to use `localhost`
# or `host.docker.internal` as appropriate.
DATABASE_URL="postgresql://postgres:postgres123@localhost:5432/management_assets?schema=public"

# JWT
JWT_SECRET="your-super-secret-jwt-key-here"
JWT_EXPIRES_IN="7d"

# Server
PORT=5001
NODE_ENV=development

# Email Configuration (optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-email-password

# Upload Configuration
MAX_FILE_SIZE=5242880
UPLOAD_PATH=uploads/
